# true  -> API calls are answered by the in-browser dev mocks (src/mocks)
# false -> API calls hit the FastAPI backend through the Vite proxy
VITE_USE_MOCK=false

# Where the Vite dev server proxies /api to. Defaults to http://127.0.0.1:8000.
# VITE_API_TARGET=http://127.0.0.1:8001
